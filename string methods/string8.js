// replace()
// replaces by taking two parameters a word to be replaced 
// and a word with whcih it has to be replaced
// replaces only first instaces,,,,,to replace all use replaceAll()

let a = "sakugar tama";

let b = a.replace("tama","mata");

console.log(b);